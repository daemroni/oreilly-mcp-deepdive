[project]
name = "oreilly-mcp-deepdive"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "daemroni", email = "damarishelenaschmid@gmail" }
]
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=2.0.0",
    "pillow>=12.3.0",
    "requests>=2.34.2",
]

[project.scripts]
oreilly-mcp-deepdive = "oreilly_mcp_deepdive:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
